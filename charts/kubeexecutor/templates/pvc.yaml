apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: {{ .Values.StorageClass }}
  resources:
    requests:
      storage: 1Gi
